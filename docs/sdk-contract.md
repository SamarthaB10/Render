<!-- Generated by scripts/generate-widget-contract.mjs. Do not edit. -->

# Render Widget Contract v1

This is the agent-facing reference generated from [the canonical JSON Schema](../contracts/render-widget-contract.v1.json). Use the schema as the source of truth; this page is a readable index.

## Widget artifact

A Widget contains a validated `manifest` and a retained `tree`. A Widget must use the SDK catalog for every primitive, provider, style, action, and capability.

## Primitives

| Kind | Summary |
| --- | --- |
| `column` | Vertical layout container |
| `row` | Horizontal layout container |
| `stack` | Layered layout container |
| `box` | General styled container |
| `glassPanel` | Theme-aware glass material panel |
| `mediaCard` | Theme-aware media surface |
| `scrollView` | Native scrollable content container |
| `spacer` | Fixed or flexible empty space |
| `divider` | Horizontal or vertical separator |
| `text` | Rendered text or provider value |
| `textField` | Editable single-line text control |
| `textArea` | Editable multi-line text control with Widget state binding |
| `textEditor` | Editable multi-line text control |
| `dateTime` | Formatted date or time value |
| `dateTimePicker` | Native date and time picker |
| `toggle` | Boolean control |
| `timer` | Persistent countdown timer |
| `taskList` | Editable persistent task list |
| `shape` | Native geometric shape |
| `icon` | Native symbol icon |
| `image` | Asset, URL, or provider-backed image |
| `button` | Native action control |
| `slider` | Persistent bounded numeric control |
| `countdown` | User-adjustable persistent countdown |
| `gauge` | Bounded value gauge |
| `progress` | Bounded progress indicator |
| `segmentedProgress` | Discrete segmented progress indicator |
| `spectrum` | Finite numeric spectrum visualization |
| `grid` | Column-based layout container |
| `gradient` | Native gradient container |
| `texture` | Built-in or asset texture surface |
| `clip` | Native clipping container |
| `transform` | Native transformed container |
| `list` | Static or provider-backed list |
| `visualizer` | Provider-backed visualizer surface |
| `youtubePlayer` | Native YouTube playback surface |

## Capabilities

- `network`
- `filesystem.read`
- `filesystem.write`

Capabilities are explicit in the manifest and require user permission when the host needs them.

## Connector scopes

- `spotify`: `user-read-private`, `user-read-playback-state`, `user-read-currently-playing`, `user-modify-playback-state`
- `reminders`: `reminders.read`, `reminders.write`

Widgets declare connector scopes in the manifest. The host owns credentials and asks the user for permission; Widget source never receives tokens.

## Providers

| Provider | Connector | Summary |
| --- | --- | --- |
| `system.cpu` | — | Current CPU utilization |
| `system.memory` | — | Current memory utilization |
| `system.time` | — | Current local time |
| `spotify.account` | `spotify` | Spotify account availability |
| `spotify.track.title` | `spotify` | Current Spotify track title |
| `spotify.track.artist` | `spotify` | Current Spotify track artist |
| `spotify.playback.isPlaying` | `spotify` | Spotify playback state |
| `spotify.playback.progress` | `spotify` | Spotify playback progress |
| `spotify.playback.volume` | `spotify` | Spotify playback volume |
| `reminders.account` | `reminders` | macOS Reminders account availability |
| `reminders.items` | `reminders` | macOS Reminders items |
| `reminders.incompleteCount` | `reminders` | Incomplete Reminders count |
| `reminders.next.title` | `reminders` | Next incomplete Reminder title |
| `reminders.next.dueDate` | `reminders` | Next incomplete Reminder due date |

## Actions

| Action | Kind | Connector | Summary |
| --- | --- | --- | --- |
| `widget.refresh` | `invoke` | — | Request a Widget refresh |
| `widget.reload` | `invoke` | — | Request a Widget reload |
| `spotify.play` | `invoke` | `spotify` | Start Spotify playback |
| `spotify.pause` | `invoke` | `spotify` | Pause Spotify playback |
| `spotify.next` | `invoke` | `spotify` | Skip to the next Spotify track |
| `spotify.previous` | `invoke` | `spotify` | Return to the previous Spotify track |
| `spotify.set-volume` | `set` | `spotify` | Set Spotify volume from 0 to 100 |
| `reminders.create` | `invoke` | `reminders` | Create a macOS Reminder |
| `reminders.update` | `invoke` | `reminders` | Update a macOS Reminder |
| `reminders.complete` | `invoke` | `reminders` | Complete or reopen a macOS Reminder |
| `reminders.delete` | `invoke` | `reminders` | Delete a macOS Reminder |

## Worker protocol

Protocol version: `1`

Message kinds: `hello`, `helloAck`, `ready`, `render`, `failure`, `shutdown`

## Contract evolution

Changes within contract version 1 must be additive. Breaking changes require a new contract version and an explicit compatibility check. Generated outputs are checked into the repository and CI must reject drift.
